# Roadmap

**AetherProof — Pulkit Kr Srivastava**

AetherProof has two implementations sharing one problem: proving that a specific
AI output was produced, unaltered, at a claimed time, verifiable by anyone offline.

This document records where each implementation stands and what 0.5.0 does about it.

---

## Current state

| | Rust (`rust-legacy`, tag `rust-legacy-v0.2.0`) | Python (`main`, 0.4.0) |
|---|---|---|
| Receipt | **128 bytes, fixed-width binary** | 646 bytes JSON |
| Signature | Ed25519 (`ed25519-dalek` 2.x) | Ed25519 (`cryptography`) |
| Kernel wire compatibility | **yes** | no |
| Session Merkle proofs | no | **yes** |
| Signed extensions (v1.2) | no | **yes** |
| Key rotation / `signing_key_id` | no | **yes** |
| Tests | 30 | 568 (93% coverage) |
| Distribution | source | PyPI |

Both are live. The Rust workspace builds clean and passes its full suite:

```
cargo build --release   # Finished in ~15s
cargo test  --release   # 30 passed; 0 failed
```

### Measured, same machine, 20,000 operations each

| Operation | Rust | Python | Ratio |
|---|---|---|---|
| Sign (build + sign) | **18.84 µs** · 53,091/s | 45.38 µs · 22,034/s | **2.4×** |
| Verify | **22.77 µs** · 43,910/s | 101.69 µs · 9,834/s | **4.5×** |
| Receipt size | **128 B** | 646 B | **5.05×** |

At 100,000 receipts that size difference is 12.8 MB against 64.6 MB.

Every Python figure was taken behind a correctness gate asserting that a valid
signature verifies, a flipped signature fails, and a tampered message fails.
Benchmarks that do not first prove the verifier is sound measure nothing.

---

## The regression 0.4.0 introduced

The Python line gained session proofs, signed extensions, and key rotation. It also,
without recording it as a cost:

1. **Grew the receipt 5×**, from a 128-byte fixed binary record to 646 bytes of JSON.
2. **Lost wire compatibility with the AetherOS kernel receipt format.** The Rust
   implementation can verify a receipt generated by the kernel; the Python one cannot.

Both are real losses. 0.5.0 exists to recover them without giving up what was gained.

---

## 0.5.0 — Rust core, Python surface

**Principle: one implementation of the signing preimage, in Rust, bound into Python.**

The most serious defect ever found in this project was a non-injective preimage
encoding (fields joined with `|`, so two different receipts could produce identical
signing bytes — a signature for one forged the other). It was fixed by
length-prefixing every field as `<len>:<field>`, with a permanent regression test.

A second implementation of that encoding is a second place for it to drift. So there
will be exactly one, in Rust, and Python will call it.

### Scope

1. **Move the wire format and crypto into `aetherproof-core`.**
   Restore the 128-byte fixed-width receipt as the canonical on-disk form. Keep JSON
   as an export format, not the source of truth.

2. **Port the session Merkle tree into Rust.**
   RFC 6962 domain separation, no odd-leaf duplication (CVE-2012-2459 class), inclusion
   proofs. This is the strongest feature of the Python line and it belongs beside the
   primitives, not above them.

3. **ML-DSA-65 (FIPS 204) as a second signature slot — BUILT, `rust/core/src/pq.rs`.**
   Not a replacement for Ed25519 — a hybrid. The 128-byte core is untouched; a trailer
   is appended carrying an ML-DSA-65 signature over the *same* signed prefix, so there
   is one signed message rather than two that could disagree.

   ```text
   [0..128]    core receipt   128-byte Ed25519 record, unchanged
   [128..136]  pq_magic       "AETHPQ01"
   [136]       pq_alg         1 = ML-DSA-65
   [137..140]  _pad           [0; 3]
   [140..144]  pq_sig_len     u32 little-endian
   [144..]     pq_sig         ML-DSA-65 over core[0..64]
   ```

   **Measured, not estimated:**

   | | Bytes |
   |---|---|
   | Core receipt (Ed25519 only) | 128 |
   | Trailer header | 16 |
   | ML-DSA-65 signature | **3,309** |
   | **Hybrid receipt total** | **3,453** |
   | ML-DSA-65 public key | 1,952 |

   | Operation | Time | Rate |
   |---|---|---|
   | ML-DSA sign | 787.83 µs | 1,269/s |
   | ML-DSA verify | 177.59 µs | 5,631/s |
   | Hybrid verify (both) | 231.85 µs | 4,313/s |

   **A 152-byte Ed25519+ML-DSA hybrid receipt is not achievable.** An ML-DSA-65
   signature is 3,309 bytes by the standard; no framing choice makes it smaller. Any
   claim of a ~152-byte post-quantum hybrid is wrong and should not be repeated.
   The smallest NIST-selected signature is Falcon-512 at 666 bytes, still five times
   the core receipt. If a small receipt matters more than a self-contained one, the
   alternative is storing a 32-byte commitment to an externally held PQ signature —
   a weaker guarantee, and it must be described as such.

   Post-quantum signing costs 42× Ed25519 on sign and 7.8× on verify. It is opt-in
   for that reason.

   Backward compatibility is verified by test, not asserted:
   attaching a trailer leaves the 128 core bytes byte-identical, a verifier that
   predates the trailer still accepts the receipt, and hybrid verification fails
   closed when the trailer is absent.

4. **PyO3 bindings, unchanged public API.**
   `Signer`, `Verifier`, `Receipt`, `Session`, and `aetherproof.auto` keep their current
   signatures. Existing code, including published integrations, continues to work.

5. **Keep 0.4.x alive as the pure-Python line** for environments that cannot build a
   native extension.

### Not in scope

- **Go.** The core is Ed25519, hashing, and serialization: no garbage collector, no
  runtime, and Rust is already faster here. A third toolchain would add a third place
  for the preimage encoding to drift, which is the exact bug class above. Go earns a
  place only if a network witness service is built, where its concurrency model helps.

### Also fix in 0.5.0

`Verifier.__init__` is type-hinted `ed25519.Ed25519PublicKey` but validates nothing.
Passing the wrong object — for example `Verifier(signer.get_public_key())`, where
`get_public_key()` already returns a `Verifier` — produces a verifier whose inner call
returns `False` without raising, so the outer `return True` runs unconditionally. The
result accepts every signature.

A verifier must fail closed. Add an `isinstance` check in the constructor.

---

## Sequencing

| Step | Gate |
|---|---|
| 1. `aetherproof-core`: 128-byte format, Ed25519, injective preimage | Rust suite green; byte-identical to `rust-legacy-v0.2.0` output |
| 2. Merkle session tree in Rust | Inclusion proofs match Python's for identical input |
| 3. ML-DSA-65 second slot — **done** | Ed25519-only receipts still verify; hybrid verifies both |
| 4. PyO3 bindings | Full 568-test Python suite green against the Rust core |
| 5. Wheels + release | Linux / macOS / Windows; `pip install aetherproof` unchanged |

Step 4 is the real gate. If the existing suite does not pass unmodified against the
Rust core, the binding is wrong and the release does not ship.

---

## Verifying the claims in this document

```bash
# Rust: build, test, benchmark
cd rust/ && cargo test --release && cargo run --release --example bench

# Python: full suite and adversarial batteries
python -m pytest tests/ -q
python tests/stress/full_audit.py
python tests/stress/isolation_audit.py
python tests/stress/industrial_stress.py
```

Every number in this file came from executing the code on one machine. None was
carried over from documentation.
